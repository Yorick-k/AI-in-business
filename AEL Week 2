AEL — the technical blueprint

1

Context reader
Reads the shared context (our target SME, quality criteria, and scope).

Add
Gathers and imports raw sources and documents.

Appraise
Critically evaluates each added source against our quality criteria.

Translate
Translates the approved source insights into the specified language.

Draft
Generates a first draft of the handbook page based on the critically evaluated sources.

Check
Strictly checks the draft for factual accuracy, source references, and other criteria from the PRD.

Publish
Formats and publishes the approved page definitively to the shared handbook repository.

2

Architectural Design: Pipeline & Contracts (Shape 6)
Our platform uses a pipeline approach where each stage takes a file and produces a file. This file serves as the contract between parts, allowing team members to build stages simultaneously.

Seam & Contract Definition
The Seam: The handoff between the Appraise part and the Draft part.

File Name: appraised_source.json

File Shape:

JSON
{
  "source_id": "string",
  "author_or_organisation": "string",
  "reliability_justification": "string (1 line)",
  "key_findings": ["list of strings"],
  "company_relevance": "string"
}

3. The line nothing may cross
To make sure there is full compliance and safeguard internal knowledge, the following requirement represents the line nothing may cross for this PRD:
Confidential SME interview data must not be sent to external AI services and must remain within the team's controlled research environment.

4. The trace to the PRD
PRD requirement:                                         Blueprint part:
Collect and add research sources                         Add
Assess relevance reliability and recency                 Appraise
Be able to work with both dutch and english sources      Translate
Preserve the meaning of Dutch source material            Translate
Select important findings                                Appraise
Create a structured first draft                          Draft
Check factual accuracy and source references             Check
Apply the quality criteria                               Check
Allow human correction and approval                      Check
Prevent fully automatic publication                      Check
Publish using the existing handbook repository           Publish
Provide a manual fallback                                Each automated stage

To be defined:
The exact implementation of the manual fallback for each automated stage will be further defined as the platform is developed.
